# typescript-lsp

TypeScript/JavaScript language server for Claude Code.

The whole capability set is declared inline in the marketplace catalog entry
(`lspServers` + `"strict": false`), so this install ships no `plugin.json` and no
`skills/`, `commands/`, or `agents/` directory. That is the upstream shape, not a
broken install.
